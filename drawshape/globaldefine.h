#ifndef GLODEFINE_H
#define GLODEFINE_H

enum EDrawToolMode {
    selection,
    rotation,
    line,
    rectangle,
    roundrect,
    ellipse,
    poly
};

enum ESelectMode {
    none,
    netSelect,
    move,
    size,
    rotate
};

#endif // GLODEFINE_H
